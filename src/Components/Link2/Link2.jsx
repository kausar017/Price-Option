
import PropTypes from 'prop-types';

const Link2 = ({data}) => {
    return (
        <div>
            <a href=""><li>{data.name}</li></a>
        </div>
    );
};

Link2.propTypes = {
    
};

export default Link2;